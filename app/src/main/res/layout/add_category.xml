<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_marginLeft="15dp"
    android:layout_marginRight="15dp"
    android:layout_marginTop="20dp"
    android:layout_marginBottom="20dp">

    <TextView android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:textSize="20sp"
        android:padding="5dp"
        android:text="Add New Category"/>

    <EditText android:id="@+id/categoryTextBox"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/title"
        android:inputType="textCapWords|text"
        android:padding="5dp"/>

    <Button android:id="@+id/cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignStart="@+id/categoryTextBox"
        android:textColor="@color/white"
        android:padding="5dp"
        android:layout_below="@id/categoryTextBox"
        android:text="Cancel"/>

    <Button android:id="@+id/addCategoryButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignEnd="@+id/categoryTextBox"
        android:padding="5dp"
        android:textColor="@color/white"
        android:layout_below="@id/categoryTextBox"
        android:text="Add"/>


</RelativeLayout>